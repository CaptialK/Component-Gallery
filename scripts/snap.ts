/**
 * Playwright snap script — TODO.
 *
 * Will iterate the registry, launch a headless browser, navigate to each
 * /c/{category}/{slug} route in both color schemes, and write
 * /public/previews/{category}__{slug}--light.png + --dark.png.
 *
 * Wired in Phase 2.
 */
console.log("TODO: wire Playwright screenshot pipeline (Phase 2)");
